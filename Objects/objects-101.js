let student = {
  name: 'Rodrigo',
  age: 30,
  location: 'Colton, CA'
};

console.log(`${student.name} is ${student.age} and lives in ${student.location}`);
student.age += 1;
console.log(`${student.name} is ${student.age} and lives in ${student.location}`);